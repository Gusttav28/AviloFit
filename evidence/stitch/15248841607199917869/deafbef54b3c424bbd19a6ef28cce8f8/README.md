# Stitch Asset Intake: AvilFit Dashboard 2

Project: Custom Analytics Dashboard
Project ID: 15248841607199917869
Screen: AvilFit Dashboard 2
Screen ID: deafbef54b3c424bbd19a6ef28cce8f8

## Attempted source

```text
https://stitch.withgoogle.com/projects/15248841607199917869/screens/deafbef54b3c424bbd19a6ef28cce8f8
```

## Files captured

- `screen.html`: Downloaded with `curl.exe -L` from the Stitch screen URL.
- `stitch-access-state.png`: Browser screenshot of the live Stitch URL after loading it in the in-app browser.

## Result

The direct `curl.exe -L` request succeeded, but the response is the generic Stitch web-app shell, not the requested screen's image or code export.

The live browser opened the exact screen URL and showed:

```text
404
This page doesn't exist or isn't shared with you.
```

The page also showed a `Sign In` control, so the current in-app browser session is not authenticated to an account with access to this Stitch project.

## Blocker

The actual screen image/code assets cannot be downloaded until the Stitch project is shared publicly, shared with the signed-in browser account, or Gustavo provides the hosted export URLs for the image and code generated by Stitch.
