import {Sparkles} from "lucide-react";import type {FitCoreInsight} from "@/features/dashboard/model";import {clampProgress} from "@/features/dashboard/format";
export function SmartInsights({data}:{data:FitCoreInsight}){const value=clampProgress(data.recoveryPercent);return <section className="fitcore-insights" aria-labelledby="smart-insights"><h2 id="smart-insights"><Sparkles size={20} aria-hidden="true"/>Smart Insights</h2><p className="fitcore-eyebrow">RECOMMENDATION</p><p>{data.recommendation}</p><div className="fitcore-recovery"><strong>Workout Recovery <em>{value}%</em></strong><div className="fitcore-progress" role="progressbar" aria-label="Workout recovery" aria-valuemin={0} aria-valuemax={100} aria-valuenow={value}><i style={{width:`${value}%`}}/></div></div><p className="fitcore-disclaimer">{data.disclaimer}</p><button type="button">View Recovery Report</button></section>}
