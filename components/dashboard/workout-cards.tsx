import {Dumbbell,Heart,PersonStanding} from "lucide-react";import type {FitCoreWorkout} from "@/features/dashboard/model";import {progressPercent} from "@/features/dashboard/format";
export function WorkoutCards({items}:{items:FitCoreWorkout[]}){return <section className="fitcore-workouts" aria-label="Workouts">{items.map(item=>{const Icon=item.kind==="run"?PersonStanding:Dumbbell,percent=progressPercent(item.progress);return <article className="fitcore-workout surface" key={item.id}><div className="fitcore-workout-title"><span className="fitcore-icon-tile"><Icon size={21} aria-hidden="true"/></span><h3>{item.name}</h3></div><div className="fitcore-workout-stats"><strong>{item.durationMinutes} min</strong><span>{item.calories} kcal</span></div><p><Heart size={20} aria-hidden="true"/> {item.averageHeartRate} bpm Avg (demo)</p><div className="fitcore-progress" role="progressbar" aria-label={`${item.name} completion`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={percent}><i style={{width:`${percent}%`}}/></div></article>})}</section>}
