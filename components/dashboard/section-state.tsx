import {Button} from "@/components/ui/button";import {Skeleton} from "@/components/ui/skeleton";
export function SectionState({state,name="Section",onRetry}:{state:"loading"|"empty"|"error";name?:string;onRetry?:()=>void}){if(state==="loading")return <><span className="sr-only">{name} is loading.</span><Skeleton/></>;return <div role={state==="error"?"alert":"status"}><p className="muted">{state==="error"?`${name} could not load. No other dashboard data was affected.`:`${name} has no available data for this day yet.`}</p>{onRetry&&<Button onClick={onRetry}>Retry section</Button>}</div>}
