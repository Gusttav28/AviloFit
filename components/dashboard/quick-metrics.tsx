import {Bed,Footprints,Flame,GlassWater} from "lucide-react";import type {FitCoreQuickMetric} from "@/features/dashboard/model";import {progressPercent} from "@/features/dashboard/format";
const icons={steps:Footprints,water:GlassWater,calories:Flame,sleep:Bed};
export function QuickMetrics({items}:{items:FitCoreQuickMetric[]}){return <section className="fitcore-metrics" aria-label="Daily quick metrics">{items.map(item=>{const Icon=icons[item.kind],percent=progressPercent(item.progress);return <article className="fitcore-metric surface" key={item.kind}><div className="fitcore-metric-top"><span className="fitcore-icon-tile"><Icon size={21} aria-hidden="true"/></span><span>{item.context}</span></div><strong>{item.value}</strong><span>{item.label}</span><div className="fitcore-progress" role="progressbar" aria-label={`${item.label} progress`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={percent}><i style={{width:`${percent}%`}}/></div></article>})}</section>}
