import {Bike,Dumbbell,Ellipsis,Filter,Footprints,PersonStanding} from "lucide-react"; import type {RecentActivity} from "@/features/activity/model"; const icons={run:PersonStanding,walk:Footprints,cycle:Bike,strength:Dumbbell};
export function RecentActivityTable({items}:{items:RecentActivity[]}){return <section className="activity-recent surface" aria-labelledby="recent-heading"><header><h2 id="recent-heading">Recent Activity</h2><button type="button" aria-label="Filter recent activity"><Filter size={18}/></button></header><div className="activity-table-scroll" tabIndex={0} aria-label="Recent activity table scroll region"><table><caption className="sr-only">Recent activity history</caption><thead><tr>{["Activity","Date","Duration","Calories","Status","Actions"].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{items.map(item=>{const Icon=icons[item.kind];return <tr key={item.label}><th scope="row"><span className="activity-row-icon"><Icon size={16}/></span>{item.label}</th><td>{item.date}</td><td>{item.duration}</td><td>{item.calories}</td><td><span className="activity-status">{item.status}</span></td><td><button type="button" aria-label={`More actions for ${item.label}`}><Ellipsis size={18}/></button></td></tr>})}</tbody></table></div></section>}
