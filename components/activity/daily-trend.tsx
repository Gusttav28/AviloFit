import {clampProgress} from "@/features/activity/format"; import type {TrendItem} from "@/features/activity/model";
export function DailyTrend({items}:{items:TrendItem[]}){return <section className="activity-trend-card surface" aria-labelledby="trend-heading"><h2 id="trend-heading">Daily Trend</h2>{items.map(item=><div className="activity-trend-row" key={item.day}><span>{item.day}{item.current&&<span className="sr-only"> Current fixture day</span>}</span><div className="activity-progress" role="progressbar" aria-label={`${item.day} activity`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={clampProgress(item.value)}><i style={{width:`${clampProgress(item.value)}%`}}/></div></div>)}</section>}
