import {Award,Dumbbell,Timer} from "lucide-react"; import {formatReferenceDate} from "@/features/activity/format"; import type {ActivityRecord} from "@/features/activity/model"; const icons={trophy:Award,timer:Timer,strength:Dumbbell};
export function PersonalRecords({items,referenceDate,timeZone}:{items:ActivityRecord[];referenceDate:string;timeZone:string}){return <section className="activity-records surface" aria-labelledby="records-heading"><h2 id="records-heading">Personal Records</h2><ol>{items.map(item=>{const Icon=icons[item.kind];return <li key={item.label}><span className="activity-record-icon"><Icon size={20} aria-hidden="true"/></span><span><b>{item.label}</b><strong>{item.value}</strong></span><time>{formatReferenceDate(item.date,referenceDate,timeZone)}</time></li>})}</ol><button type="button">View All Achievements</button></section>}
