import {Bike,Dumbbell,Footprints,PersonStanding} from "lucide-react"; import type {ActivitySummaryMetric} from "@/features/activity/model";
const icons={run:PersonStanding,walk:Footprints,cycle:Bike,strength:Dumbbell};
export function ActivitySummaryCards({items}:{items:ActivitySummaryMetric[]}){return <section className="activity-summary-grid" aria-labelledby="activity-summary-title"><h2 id="activity-summary-title" className="sr-only">Activity summary</h2>{items.map(item=>{const Icon=icons[item.kind];return <article className="activity-summary-card" key={item.label}><div className="activity-summary-card-head"><span className="activity-summary-icon"><Icon size={22} aria-hidden="true"/></span><span className="activity-trend">{item.trend}</span></div><h3>{item.label}</h3><p><strong>{item.value}</strong> <span>{item.unit}</span></p></article>})}</section>}
