import {render,screen} from "@testing-library/react"; import {describe,expect,it} from "vitest"; import {AiPerformanceCoach} from "@/components/activity/ai-performance-coach";
describe("Activity AI safety",()=>{it("labels fixture guidance as synthetic and non-medical",()=>{render(<AiPerformanceCoach data={{title:"AI Performance Coach",recommendation:"General wellness practice guidance.",disclosure:"Synthetic demo guidance for general wellness only. This is not medical advice."}}/>);expect(screen.getByText(/synthetic demo coach/i)).toBeInTheDocument();expect(screen.getByText(/not medical advice/i)).toBeInTheDocument();expect(screen.queryByText(/diagnos|treat|guarantee|connected device/i)).not.toBeInTheDocument();});});
