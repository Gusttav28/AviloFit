import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it} from "vitest";import {DashboardScreen} from "@/components/dashboard/dashboard-screen";import {fixtureDashboardProvider,fixtureWithSectionState} from "@/features/dashboard/fixture-dashboard-provider";
describe("FitCore dashboard composition",()=>{it("renders the approved main-content hierarchy without the outgoing dashboard",async()=>{const model=await fixtureDashboardProvider.getDashboard();render(<DashboardScreen model={model}/>);expect(screen.getByRole("heading",{name:"Weekly Performance"})).toBeInTheDocument();expect(screen.getByRole("searchbox",{name:"Search analytics"})).toBeInTheDocument();for(const label of ["Steps Taken","Water Intake","kcal Left","Deep Sleep","Today's Meals","Morning Run","Strength Training","Smart Insights"])expect(screen.getByText(label)).toBeInTheDocument();expect(screen.queryByText("Keep it up, Uzui!")).not.toBeInTheDocument();expect(screen.queryByText("Summary")).not.toBeInTheDocument();expect(screen.queryByText("Ask anything to Avilo AI")).not.toBeInTheDocument()});it("keeps search and calendar selection local",async()=>{const model=await fixtureDashboardProvider.getDashboard();render(<DashboardScreen model={model}/>);const search=screen.getByRole("searchbox",{name:"Search analytics"});fireEvent.change(search,{target:{value:"protein"}});expect(search).toHaveValue("protein");fireEvent.click(screen.getByRole("button",{name:/July 22, 2026/}));expect(screen.getByText("Meal Prep Sunday")).toBeInTheDocument();});it("recovers redesigned sections independently",()=>{const model=fixtureWithSectionState("metrics","error");render(<DashboardScreen model={model}/>);expect(screen.getByRole("region",{name:"metrics state"})).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Retry section"}));expect(screen.getByRole("region",{name:"Daily quick metrics"})).toBeInTheDocument()})});
